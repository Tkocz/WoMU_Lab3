<Page
    x:Class="WoMU_Lab3.AddWallView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WoMU_Lab3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" >
        <Image Source="../Assets/images/triforce_bg2.png"/>
        <ScrollViewer         Name="listView"
                          ScrollViewer.HorizontalScrollMode="Disabled"
                          ScrollViewer.VerticalScrollMode="Enabled"
                          ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden">
            <ListView>
                <Button Name="CameraButtonButton" MaxWidth="200" MaxHeight="200" Tapped="CameraButton">
                    <SymbolIcon Symbol="Camera" Height="100" Width="100"/>
                </Button>
                <TextBox x:Name="titleBox" Text="Titel" MaxLength="30"/>
                <TextBox x:Name="detailsBox" Text="Detaljerad Beskrivning" Height="110" Width="300"/>
                <GridView>
                    <Button Content="Back" Padding="50,10,50,10" Margin="5,50,5,5">
                    </Button>
                    <Button Content="Save" Padding="50,10,50,10" Margin="5,50,5,5">
                    </Button>
                </GridView>
            </ListView>
        </ScrollViewer>
    </Grid>
</Page>
